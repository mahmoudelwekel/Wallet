@model App.UI.Models.ExampleModels.CreateExampleReqModel 

@{
    ViewData["Title"] ="Create Tranaction";
}

<h1>@ViewData["Title"]</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            @Html.AntiForgeryToken()
            <div class="form-group mb-3">
                <label asp-for="ExampleAmount" class="control-label"></label>
                <input asp-for="ExampleAmount" class="form-control" type="number" />
                <span asp-validation-for="ExampleAmount" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="ExampleToUserPhone" class="control-label"></label>
                <input asp-for="ExampleToUserPhone" class="form-control" />
                <span asp-validation-for="ExampleToUserPhone" class="text-danger"></span>
            </div>

            
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <input type="submit" value="Create" class="btn btn-primary me-2" />
                @*<a asp-action="Index" class="btn btn-secondary">@Edrak.Enums.Labels.Cancel </a>*@
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
